
import Image from "next/image";
import styles from './Navbar.module.scss'

const Navbar = () => {
    return (
        <div className={`${styles.wrapper} bg-white h-16 border border-b w-full`}>
            Navbar
        </div>
    )
}

export default Navbar;